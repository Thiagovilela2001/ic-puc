# =============================================================================
# Dependências do sistema de identificação de galpões culturais — São Paulo
# =============================================================================
# Ambiente Conda recomendado:
#   conda create -n relatorio python=3.12 -y
#   conda activate relatorio
#   python -m pip install --upgrade pip
#   python -m pip install -r requirements.txt
#
# Opcional, se quiser instalar o pacote local em modo editável:
#   python -m pip install -e .
# =============================================================================

# --- Core -------------------------------------------------------------------
crewai[tools]==1.9.3          # framework multi-agente + ferramentas integradas

# --- Busca web ---------------------------------------------------------------
# SerperDevTool (recomendado): configure SERPER_API_KEY no .env
# Já incluída no crewai[tools], sem instalação extra necessária.

# DuckDuckGo (fallback gratuito, sem chave de API):
duckduckgo-search>=6.0.0     # usado diretamente via duckduckgo_search.DDGS

# --- Utilitários ------------------------------------------------------------
python-dotenv>=1.0.0         # carregamento de variáveis de ambiente do .env
pydantic>=2.0.0              # schemas de validação dos modelos de saída
